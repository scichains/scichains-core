# Note: This package name `pyalgart_executors_core` intentionally uses long, Java-style
# namespace naming for clarity and consistency across independently pluggable SciChains extensions.
# This helps avoid conflicts when multiple packages are installed in different extensions.
